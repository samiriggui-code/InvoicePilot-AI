# E-mails InvoicePilot (React Email)

Prévisualisation locale :

```bash
npm run email:dev
```

→ http://localhost:3005

Envoi réel (dev) : SMTP Mailpit Laragon `127.0.0.1:1025` — UI http://127.0.0.1:8025

Templates :
- `team-invite.tsx` — invitation collègue
- `welcome.tsx` — bienvenue inscription
- `password-reset.tsx` — reset MDP (prêt à brancher)
- `trial-ending.tsx` — rappel fin d’essai (prêt à brancher)
